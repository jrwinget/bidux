# Fallback concepts data for emergency use

Provides minimal fallback data if external file cannot be loaded. Only
used as last resort to prevent package failure.

## Usage

``` r
get_fallback_concepts_data()
```

## Value

A tibble with minimal concepts data
